info:
  contact: {}
paths:
  /getUsers:
    get:
      responses:
        "200":
          description: code","message"}
          schema:
            type: string
      tags:
      - 获取用户列表
  /index:
    get:
      responses:
        "200":
          description: OK
          schema:
            type: string
      tags:
      - 首页
swagger: "2.0"
